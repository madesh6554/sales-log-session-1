{
  "name": "forgelite",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal sales log — Express, Postgres, plain HTML/CSS/JS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "playwright test"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
